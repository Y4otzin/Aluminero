# Web Framework
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# Database
sqlalchemy==2.0.27
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Auth & Security
python-jose[cryptography]==3.3.0
bcrypt>=4.0.0
python-dotenv==1.0.1

# Email
resend==0.7.0

# Imágenes
Pillow==10.2.0

# Boceto vectorial (SVG) — reemplaza IA externa
svgwrite>=1.4.3
cairosvg>=2.7.0       # rasterizar SVG → PNG si se necesita en PDF

# PDF
weasyprint==61.2

# Validación
pydantic==2.6.1
pydantic-settings==2.1.0
email-validator==2.1.0

# Supabase Storage (vía httpx REST API, no usa supabase-py)
httpx==0.27.0

# Testing
pytest==8.0.0
pytest-cov==4.1.0
pytest-asyncio==0.23.5

# Monitoreo
sentry-sdk[fastapi]==1.40.3

# Utils
python-dateutil==2.8.2
